;; (liii tree) 模块函数分类索引
;;
;; `(liii tree)` 提供树结构（嵌套列表）相关的操作函数。

;; ==== 使用说明 ====
;;   (import (liii tree))

;; ==== 函数分类索引 ====

;; 一、树结构检查
;;   tree-cyclic?         - 检查树结构是否有循环
;;   tree-memq            - 检查对象是否存在于树结构中
;;   tree-set-memq        - 检查列表形式的一组符号中是否有任意符号存在于树中

;; 二、树结构计数
;;   tree-count           - 统计对象在树结构中出现次数
;;   tree-leaves          - 返回树的叶子节点数
