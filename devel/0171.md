# [0171] 迁移 s7.c 中的 list 相关函数到 s7_liii_list.h/c

## 任务相关的代码文件
- src/s7.c
- src/s7_liii_list.h
- src/s7_liii_list.c
- src/s7_internal_helpers.h
- tests/scheme/base/make-list-test.scm

## 如何测试
```bash
xmake b goldfish
bin/gf fmt
bin/gf tests/scheme/base/make-list-test.scm
bin/gf tests/test_all.scm
```

## 迁移计划与进度

遵循“一个函数或一组函数一个 commit，小步快跑，每次迁移都补充单元测试”原则：

- [x] Step 1: 迁移 `make-list` 包装函数 (`g_make_list`)
- [x] Step 2: 迁移 `list-ref` 常量索引特化函数 (`g_list_ref_at_0`, `g_list_ref_at_1`, `g_list_ref_at_2`)
- [x] Step 3: 迁移 `list` 参数特化与优化函数 (`list_p_p`, `list_p_pp`, `list_p_ppp`)
- [x] Step 4: 迁移 `memq` 家族与优化派发器 (`s7_memq`, `g_memq`, `g_memq_3`, `g_memq_any`)
- [ ] Step 5: 迁移列表拼接核心算法 (`g_list_append`, `append_in_place`)
- [ ] Step 6: 迁移列表反转算法组 (`s7_reverse`, `reverse_pair_in_place`, `any_list_reverse_in_place`, `reverse_in_place_unchecked`)

## 2026-09-09 迁移 s7_memq, g_memq, g_memq_3, g_memq_any 到 s7_liii_list.c

### What
1. 在 `src/s7_liii_list.h` 声明 `g_memq`, `g_memq_3`, `g_memq_any`。
2. 在 `src/s7_liii_list.c` 中实现 `s7_memq`（带环形列表快慢指针检测）、`g_memq`、`g_memq_3` 和 `g_memq_any`。
3. 从 `src/s7.c` 中移除原实现并替换为迁移注释。

### Why
`memq` 依赖纯粹的对象指针比对与列表遍历逻辑，收归 `s7_liii_list.c` 实现更整洁的模块封装。

## 2026-09-09 补充 memq 特化与环形列表单元测试

### What
在 `tests/scheme/base/memq-test.scm` 中增加点对结构尾部匹配规则、长度 2/3/4/8 优化分支覆盖、环形列表环路检测以及错误参数类型的单元测试。

### Why
遵循 TDD 原则，在迁移 `memq` 家族函数前建立健全的测试屏障。

## 2026-09-09 迁移 list_p_p, list_p_pp, list_p_ppp 到 s7_liii_list.c

### What
1. 在 `src/s7_internal_helpers.h` 中添加 `s7i_set_sc_value` 并在 `src/s7.c` 中实现。
2. 在 `src/s7_liii_list.h` 声明 `list_p_p`, `list_p_pp`, `list_p_ppp`。
3. 在 `src/s7_liii_list.c` 中实现这三个优化器 typed-arg 函数。
4. 从 `src/s7.c` 中移除原定义并改为迁移注释。

### Why
将优化器直接比较和派发的 1/2/3 参 `list` 特化函数与其它列表 typed-arg 函数一并收归 `s7_liii_list.c` 管理。

## 2026-09-09 补充 list 参数特化与内联调用单元测试

### What
在 `tests/scheme/base/list-test.scm` 中增加针对 0、1、2、3、4 参数特化以及局部绑定、闭包环境内调用 `list` 的针对性测试用例，覆盖 `list_p_p`、`list_p_pp`、`list_p_ppp` 路径。

### Why
遵循 TDD 原则，在迁移 `list_p_*` 特化函数前建立完备的测试保护。

## 2026-09-09 迁移 g_list_ref_at_0/1/2 到 s7_liii_list.c

### What
1. 在 `src/s7_liii_list.h` 声明 `g_list_ref_at_0`, `g_list_ref_at_1`, `g_list_ref_at_2`。
2. 在 `src/s7_liii_list.c` 中实现这三个优化器特化函数，统一使用 `s7i_method_or_bust` 与 `s7_out_of_range_error`。
3. 从 `src/s7.c` 中移除原 `static` 定义。

### Why
解耦常量索引下的优化路径派发函数，与主函数 `g_list_ref` 聚合于 `s7_liii_list.c`，进一步精简 `s7.c`。

## 2026-09-09 补充 list-ref 常量索引单元测试

### What
在 `tests/scheme/base/list-ref-test.scm` 中增加常量索引 0、1、2 在不同长度列表、点对、空列表和非列表对象上的读取、越界和类型错误用例，为 `g_list_ref_at_0/1/2` 迁移提供完整的测试保护。

### Why
遵循 TDD 原则，在迁移优化器特化分发函数之前先行建立覆盖保障。

## 2026-09-09 迁移 g_make_list 到 s7_liii_list.c

### What
1. 在 `src/s7_liii_list.h` 声明 `g_make_list`。
2. 在 `src/s7_liii_list.c` 中实现 `g_make_list`。
3. 从 `src/s7.c` 中移除 `static s7_pointer g_make_list`。
4. 补充 `tests/scheme/base/make-list-test.scm` 异常用例。

### Why
解耦内置列表构造函数，降低 `s7.c` 体积。
