# [216_44] 拆分 read-test.scm 到 tests/scheme/read/ 目录

## 任务相关的代码文件
- tests/goldfish/scheme/read-test.scm (删除)
- tests/scheme/read-test.scm (新建，顶层入口)
- tests/scheme/read/read-test.scm (新建，read 函数测试)

## 如何测试
```bash
xmake b goldfish
bin/gf fix tests/scheme/read/
bin/gf fix tests/scheme/read-test.scm
bin/gf tests/scheme/read/read-test.scm
```

## 2026-04-01 拆分 read-test.scm

### What
将原有的 `tests/goldfish/scheme/read-test.scm` 文件按照 Unit_Test_ZH.md 规范拆分为两层结构：

1. **创建顶层入口文件** `tests/scheme/read-test.scm`
   - 模块导览说明
   - 常见用法示例
   - 函数分类索引

2. **创建函数测试文件** `tests/scheme/read/read-test.scm`
   - read 函数的完整测试用例（12个检查点）

3. **删除旧测试文件** `tests/goldfish/scheme/read-test.scm`

### Why
- 遵循项目新的测试组织规范
- 每个函数独立测试文件，便于维护和定位问题
- 顶层入口文件提供模块整体说明和快速导航

### How
- 严格按照 Unit_Test_ZH.md 的两层结构规范
- 保留原有测试逻辑和用例
- 为测试文件添加完整的文档字符串（语法、参数、返回值、描述）
- 顶层入口文件示例代码使用注释形式，避免执行错误
