# [0182] 新建 (liii generator) 并添加测试和文档

## 任务相关的代码文件
- goldfish/liii/generator.scm
- tests/liii/generator-test.scm
- tests/liii/generator/*-test.scm
- devel/0182.md

## 如何测试
```bash
bin/gf fmt
bin/gf tests/liii/generator/generator-test.scm
bin/gf doc --build-json
bin/gf doc liii/generator
```

## 2026-09-15 新建 (liii generator) 库及 54 个函数的测试与文档

### What
1. 新建 `goldfish/liii/generator.scm`，导出 SRFI-158 中的全部 54 个函数。
2. 在 `tests/liii/generator/` 下为 54 个函数逐一编写符合 Golddoc 格式的文档与单元测试（文档中省略示例小节，由下方的单元测试直接承载用例演示）。
3. 执行 `bin/gf fmt` 格式化代码，运行所有测试用例并验证通过。
4. 执行 `bin/gf doc --build-json` 重建文档索引，并验证文档可正常检索。

### Why
完善 Goldfish Scheme 的生成器与累加器标准库支持，将 SRFI-158 中的生成器构造、转换、组合变换、折叠查找与累加器等能力收录到 `(liii generator)` 中，并提供完整的中文文档与测试覆盖。

### How
通过新建 `(liii generator)` 库导入并重新导出 `(srfi srfi-158)` 的全部符号，并按照 Golddoc 的命名映射规范（如 `->` 映射为 `-to-`、`!` 映射为 `-bang`、`*` 映射为 `-star`）建立 54 个独立的测试文档文件。
