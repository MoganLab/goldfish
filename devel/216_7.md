# [216_7] 拆分 base-test.scm 到 tests/liii/base/ 目录

## 2026-04-22 新增 (scheme base) 单元测试格式文档

### What
为 R7RS Scheme 定义的 `(scheme base)` 库中的函数新增/补全单元测试格式的文档注释。

当前 `tests/scheme/base/` 目录下已有 142 个独立测试文件，每个文件对应一个 `(scheme base)` 函数。本次任务目标是：

1. **补全缺失的文档注释**：确保每个测试文件都包含完整的文档块
   - 函数名与中文描述
   - 语法说明
   - 参数列表与类型
   - 返回值类型与说明
   - 注意事项
   - 错误处理说明

2. **统一文档格式**：确保所有测试文件遵循统一的注释格式规范

3. **补充测试用例**：在完善文档的同时，补充边界条件和异常场景的测试用例

### 新增/补全的单元测试文件

本次提交为以下函数新增了独立的单元测试文件（每个函数一个 `.scm` 文件）：

**数值比较与转换（7个）**
- `=` `<` `>` `<=` `>=` `exact` `inexact`

**列表操作（2个）**
- `cons` `cdar`

**字符串操作（9个）**
- `string` `string-append` `string-fill!` `string->list` `list->string`
- `string<?` `string>?` `string<=?` `string>=?`

**向量操作（14个）**
- `vector?` `make-vector` `vector` `vector-length` `vector-ref` `vector-set!`
- `vector->list` `list->vector` `vector->string` `string->vector`
- `vector-copy` `vector-copy!` `vector-fill!` `vector-append`

**字节向量操作（3个）**
- `bytevector-append` `utf8->string` `string->utf8` `bytevector-advance-utf8`

**端口操作（2个）**
- `binary-port?` `textual-port?`

**控制与异常（3个）**
- `string-for-each` `vector-for-each` `guard`

**S7 内置 / R7RS base 常用函数（约 30 个）**
- `apply` `procedure?` `eof-object?` `for-each` `expt`
- `values` `call-with-values` `dynamic-wind` `call/cc` `error`
- `input-port?` `output-port?` `current-input-port` `current-output-port` `current-error-port`
- `close-input-port` `close-output-port` `open-input-file` `open-output-file`
- `with-input-from-file` `with-output-to-file` `read-char` `peek-char` `newline`
- `display` `write-char` `write-string` `flush-output-port` `read-string` `char-ready?`
- `unless` `set!` `begin` `define` `quasiquote`

### 发现的问题

1. **`open-binary-input-file` / `open-binary-output-file`**：在 `goldfish/scheme/base.scm` 的 export 列表中，但无实际绑定（仅在 `(scheme file)` 中定义），导入 `(scheme base)` 后调用会抛出 `unbound-variable`。

2. **`bytevector-copy!`**：未在 `goldfish/scheme/base.scm` 中实现，属于 `(scheme base)` 缺失实现。

3. **以下函数在 `(scheme base)` 中未绑定**：`delay` `delay-force` `parameterize` `let*-values` `define-syntax` `syntax-rules` `raise-continuable` `with-exception-handler` `features` `open-input-bytevector` `open-output-bytevector` `get-output-bytevector` `read-u8` `write-u8` `peek-u8` `u8-ready?` `read-bytevector` `read-bytevector!` `write-bytevector` `write-shared` `write-simple`。

4. **跨平台数值比较行为不一致**（调研记录，参见 [x_y.md](x_y.md) 模板格式）：
   - **问题**：`tests/scheme/base/eq-sign-test.scm` 第 59 行 `(= 1/3 0.3333333333333333)` 在 Linux 和 macOS/Windows 上行为不一致
   - **数值类型**：`1/3` 是 **exact**（精确有理数），`0.3333333333333333` 是 **inexact**（不精确浮点数）
   - **Linux**：正确返回 `#f`（精确数 `1/3` 与不精确数 `0.3333333333333333` 不相等）
   - **macOS/Windows**：错误返回 `#t`
   - **S7 Scheme 的比较逻辑**：
     - 当比较 **T_RATIO**（有理数）和 **T_REAL**（浮点数）时，S7 的做法是将有理数转换为浮点数后比较
     - 代码路径：`src/s7.c:18957` `case T_REAL: return(fraction(x) == real(y));`
     - `fraction` 宏定义：`src/s7.c:3755` `#define fraction(p) (((long_double)numerator(p)) / ((long_double)denominator(p)))`
   - **根本原因**：`long_double` 在不同平台上的精度不同
     - **Linux**：`long double` 通常是 80 位或 128 位扩展精度，能区分 `1/3` 和 `0.3333333333333333`
     - **macOS (ARM64)** 和 **Windows**：`long double` 只有 64 位（与 `double` 相同），无法区分两者
   - **为什么有些 exact/inexact 比较是正确的**：
     - 如 `(= 1/2 0.5)` => `#t`，因为 `1/2` 可以在二进制浮点数中**精确表示**
     - 但 `1/3` 在二进制中是无限循环小数，无法精确表示，`0.3333333333333333` 只是近似值
   - **状态**：当前通过 `(os-linux?)` 在测试中进行平台适配，但根本问题需要 S7 Scheme 层面修复。

### Why
1. 完整的文档注释便于后续通过 `gf doc` 工具自动生成函数文档
2. 统一格式后，AI 和人类开发者都能快速理解每个函数的用途和行为
3. 为 Goldfish Scheme 的标准库文档化奠定基础

### How
1. 遍历 `tests/scheme/base/` 目录下所有测试文件
2. 检查每个文件的文档注释完整性
3. 按照既定格式补全或修正文档块
4. 运行测试验证所有文件仍能通过

### 验证
```bash
# 构建
xmake b goldfish

# 运行所有 scheme/base 测试
bin/gf test tests/scheme/base

# 验证文档索引
bin/gf doc --build-json
```

---

## 2026-04-04 拆分 and-test.scm
- commit: [a5699920](https://gitee.com/XmacsLabs/goldfish/commit/a5699920)

---

## 2026-04-04 修复 tests/scheme/base/ 目录下 import 问题
- commit: [dd28963f](https://gitee.com/XmacsLabs/goldfish/commit/dd28963f)

---

## 2026-04-06 修复 R7RS 6.5 函数多库导出问题

### What
修复 R7RS 6.5 (Symbols) 函数在 `(liii base)` 和 `(scheme base)` 中重复导出的问题。

涉及的函数：
- `symbol?` - 判断是否为符号
- `symbol=?` - 符号相等判断
- `string->symbol` - 字符串转符号
- `symbol->string` - 符号转字符串

### Why
1. 这些函数已经在 `(scheme base)` 中导出
2. `(liii base)` 通过 `(import (scheme base))` 继承了这些函数
3. 重复导出导致 `bin/gf doc "函数名"` 显示函数在多个库中实现
4. 与之前 R7RS 6.4 (list) 的修复保持一致

### How
从 `(liii base)` 的 export 列表中移除 R7RS 6.5 函数的显式导出。

### 验证
```bash
# 构建
xmake b goldfish

# 运行相关测试
bin/gf tests/scheme/base/symbol-p-test.scm
bin/gf tests/scheme/base/symbol-eq-p-test.scm
bin/gf tests/scheme/base/string-to-symbol-test.scm
bin/gf tests/scheme/base/symbol-to-string-test.scm
```

---
## 任务相关的代码文件
- tests/goldfish/liii/base-test.scm (源文件)
- tests/liii/base-test.scm (迁移后的入口说明文件)
- tests/liii/base/*.scm (目标目录)

## 如何测试

```bash
# 运行单个测试文件
bin/goldfish -m r7rs tests/liii/base/eqv-p-test.scm

# 运行整个 base 测试目录
for f in $(find tests/liii/base -maxdepth 1 -type f | sort); do
  bin/goldfish -m r7rs "$f" || exit 1
done
```

## 2026-03-30 拆分 base-test.scm
- commit: [00483be6](https://gitee.com/XmacsLabs/goldfish/commit/00483be6)
