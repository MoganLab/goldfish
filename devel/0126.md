# [0126] 更新 README.md 和 README_ZH.md

## 任务相关的代码文件
- README.md
- README_ZH.md

## 如何测试
文档更新，无需运行测试。已用以下方式验证：
+ `bin/gf` 输出与 README 中的子命令表、帮助信息示例核对
+ `bin/gf -e '(begin (import ...))'` 验证新增库条目的 import 及示例函数名（grep define 确认）

## 2026-08-19 更新 README.md 和 README_ZH.md

### What
1. 子命令表与 `gf` 实际帮助输出同步：新增 `code`、`doc`、`fmt`、`pr`、`source`，修正 `fix` 的描述（按缩进修正括号，而非格式化）
2. "显示帮助"一节的示例输出更新为当前版本的实际输出
3. 标准库表新增 8 个已发布库：`(liii njson)`、`(liii subprocess)`、`(liii base64)`、`(liii hashlib)`、`(liii unicode)`、`(liii time)`、`(liii argparse)`

### Why
README 中的子命令表和标准库表落后于代码实际状态，新用户无法从 README 了解 `gf doc`、`gf fmt`、`gf pr` 等实用子命令和新增标准库。
