# [200_59] 从 `(liii base)` 中移除 `let1`

## 如何测试

### 1. 查看 `(liii base)` 源码，确认 `let1` 已不在导出列表中
```bash
./bin/gf source liii/base
```

预期在 `; Extra structure` 一段看到：

```text
typed-lambda
```

并且不再看到：

```text
let1
```

### 2. 重建 `gf doc` 的函数索引
```bash
./bin/gf doc --build-json
```

预期输出：

```text
Built function index: /home/wumo/work/work/goldfish/tests/function-library-index.json
```

### 3. 查询 `let1`
```bash
./bin/gf doc "let1"
```

预期不再显示：

```text
Function let1 is exported in:
  (liii base)
```

当前预期会走未命中分支，输出类似：

```text
No exact match for function: let1
```

### 4. 运行 `liii/base` 目录测试
```bash
./bin/gf test tests/liii/base
```

预期测试全部通过。

## 说明

本次改动将 `let1` 从 `(liii base)` 中移除，范围包括：

- 从 `goldfish/liii/base.scm` 的 `export` 列表中删除 `let1`
- 删除 `goldfish/liii/base.scm` 中 `let1` 的宏定义
- 从 `tests/liii/base-test.scm` 的模块级说明中删除 `let1`

`let1` 原本只是单绑定 `let` 的语法糖：

```scheme
(let1 x expr body...)
```

等价于：

```scheme
(let ((x expr))
  body...)
```

当前仓库内部已经不再实际使用它，因此这次直接收缩 `(liii base)` 的公开接口，统一使用标准 `let`。

## 2026/04/03 从 `(liii base)` 中移除 `let1`
