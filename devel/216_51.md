# [216_51] 为 (liii chez) 新增测试

## 任务相关的代码文件
- goldfish/liii/chez.scm - (liii chez) 模块实现
- tests/liii/chez-test.scm - 测试索引文件
- tests/liii/chez/ - 各函数测试用例目录

## 如何测试
```bash
# 1. 构建
xmake b goldfish

# 2. 运行测试
bin/gf tests/liii/chez-test.scm
```

## 2026-04-03 为 (liii chez) 新增 chez-test.scm 和函数测试用例

### What
1. 创建 `tests/liii/chez/` 目录存放各函数测试用例
2. 创建 `tests/liii/chez/atom-p-test.scm` - `atom?` 函数测试
3. 创建 `tests/liii/chez-test.scm` - 函数索引文档，说明 (liii chez) 用于 Chez Scheme 兼容

### Why
- (liii chez) 模块目前只有 `atom?` 一个函数
- 需要为模块建立完整的测试结构，便于后续扩展
- chez-test.scm 作为函数索引文档，说明模块用途

### How
参考现有 tests/liii/ 下的目录结构模式：
- 子目录如 `tests/liii/list/` 下按函数分文件存放测试
- 索引文件如 `tests/liii/list-test.scm` 作为函数分类索引文档

### 测试结果
```
$ bin/gf tests/liii/chez/atom-p-test.scm
; *** checks *** : 23 correct, 0 failed.
```
