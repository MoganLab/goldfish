# [216_45] 拆分 write-test.scm 测试文件

## 任务相关的代码文件
- tests/goldfish/scheme/write-test.scm (待删除)
- tests/scheme/write-test.scm (顶层入口说明文件)
- tests/scheme/write/*.scm (6个独立测试文件)

## 如何测试
```bash
xmake b goldfish
bin/gf fix tests/scheme/write/
bin/gf fix tests/scheme/write-test.scm

# 运行所有拆分后的测试
for f in $(find tests/scheme/write -maxdepth 1 -type f -name "*-test.scm" | sort); do
  echo "Testing: $f"
  bin/gf "$f" || exit 1
done
```

## 2026-04-01 拆分 write-test.scm

### What
将 tests/goldfish/scheme/write-test.scm 按照 Unit_Test_ZH.md 规范拆分到 tests/scheme/write/ 目录：

1. 创建 tests/scheme/write-test.scm 作为顶层入口说明文件
2. 创建 6 个独立的函数测试文件：
   - write-test.scm - write 函数测试
   - display-test.scm - display 函数测试
   - newline-test.scm - newline 函数测试
   - write-char-test.scm - write-char 函数测试
   - write-simple-test.scm - write-simple 函数测试
   - write-shared-test.scm - write-shared 函数测试
3. 删除旧的 tests/goldfish/scheme/write-test.scm 文件

### Why
- 按照项目规范统一测试结构
- 每个函数独立测试文件，便于维护和定位问题
- 顶层入口文件作为模块导览，方便用户快速了解模块功能

### How
- 严格遵循两层结构：顶层入口（无check）+ 子目录单个测试文件（有check）
- 每个测试文件包含完整的函数文档注释和测试用例
- 测试文件以 (check-report) 结尾
