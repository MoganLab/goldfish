# [0014] 新增 bin/gf code 子命令与 (liii path) 文件复制功能

## 相关文档
- [dddd.md](dddd.md) - 任务文档模板

## 任务相关的代码文件
- `gfproject.json` - gf 子命令配置入口
- `tools/code/liii/goldcode.scm` - gf code 子命令实现
- `src/goldfish.hpp` - C 层 `g_path-copy` 函数
- `goldfish/liii/path.scm` - `path-copy`、`path-copy-into`、`path-clone`
- `tests/liii/path/path-clone-test.scm` - path-clone 测试
- `tests/liii/path/path-copy-test.scm` - path-copy 测试
- `tests/liii/path/path-copy-into-test.scm` - path-copy-into 测试

## 如何测试

### 确定性测试（单元测试）
```bash
xmake b goldfish
bin/gf tests/liii/path/path-clone-test.scm
bin/gf tests/liii/path/path-copy-test.scm
bin/gf tests/liii/path/path-copy-into-test.scm
```

### 非确定性测试（文档验证）
```bash
bin/gf doc "path-copy"
bin/gf doc "path-copy-into"
```

## 2026/05/04 任务规划

### What
1. 新增 `bin/gf code` 子命令，将 Claude Code 启动流程集成到 `gf` 工具链中：
   - 同步 `hooks/pre-commit` 到 `.git/hooks/pre-commit`
   - 可选检查当前分支是否为 `main`
   - 在 `main` 分支时自动 `git pull origin main`
   - 启动 Claude Code (`claude` 命令)
2. 在 `(liii path)` 中实现 `path-copy`（文件复制）和 `path-copy-into`（复制到目录），参考 Python 的 `Path.copy` 和 `Path.copy_into`。
3. 将原 `(liii path)` 中复制 path 对象的 `path-copy` 改名为 `path-clone`，避免命名冲突。
4. 在 C 层（`src/goldfish.hpp`）新增 `g_path-copy` 函数，使用 tbox 文件 API 实现二进制安全的文件复制。

### Why
- 将 Claude Code 启动流程集成为 `gf code` 子命令可以统一 CLI 入口，减少维护成本。
- `(liii path)` 缺少文件复制功能，`path-copy` 和 `path-copy-into` 是常见的路径操作需求，对标 Python `pathlib` 的 API 设计。
- 原 `path-copy` 复制的是 path 记录对象而非文件，与大多数用户直觉不符。将其改名为 `path-clone` 后，`path-copy` 可以自然地承担文件复制的语义。

### How
1. **`bin/gf code` 子命令**：
   - 在 `gfproject.json` 中注册 `code` 子命令，指向 `liii/goldcode` 模块。
   - 在 `tools/code/liii/goldcode.scm` 中实现 `run-goldcode`，使用 `(liii os)` 的 `os-call` 执行 git 命令，使用 `(liii path)` 的 `path-read-text`/`path-write-text` 或新的 `path-copy` 同步 hook 文件。
   - `launch-claude` 使用 `g_system "claude"` 启动 Claude Code。

2. **`path-copy` 与 `path-copy-into`**：
   - C 层：在 `src/goldfish.hpp` 的 `glue_liii_path` 中新增 `glue_path_copy`，函数 `f_path_copy` 使用 `tb_file_init`、`tb_file_read`、`tb_file_writ` 实现源文件到目标文件的逐块复制。
   - Scheme 层：在 `goldfish/liii/path.scm` 中导出 `path-clone`、`path-copy`、`path-copy-into`。
     - `path-clone`：原 `path-copy` 的逻辑，复制 path 记录对象。
     - `path-copy`：调用 `g_path-copy`，复制文件内容。
     - `path-copy-into`：提取源文件文件名，拼接目标目录，再调用 `path-copy`。

3. **测试迁移**：
   - 将 `tests/liii/path/path-copy-test.scm` 改名为 `path-clone-test.scm`，断言中的 `path-copy` 替换为 `path-clone`。
   - 新增 `path-copy-test.scm` 和 `path-copy-into-test.scm`，覆盖文件复制的基本场景和目录复制场景。
