# [0076] gf fmt 为 clang-format 添加文件缓存

## 任务相关的代码文件
- `tools/fmt/liii/cpp-fmt.scm` - C++ 格式化处理器，增加缓存命中/写入逻辑
- `tools/fmt/liii/scheme-fmt.scm` - Scheme 格式化处理器，复用新的共享缓存模块
- `tools/fmt/liii/goldfmt-cache.scm` - 新增：共享格式化缓存模块
- `tools/fmt/tests/liii/goldfmt-cache-test.scm` - 新增：缓存模块单元测试
- `goldfish/liii/path.scm` - 新增 `path-contains?` 函数
- `tests/liii/path/path-contains-p-test.scm` - 新增 `path-contains?` 单元测试

## 如何测试

### 3.1 确定性测试（单元测试）
```bash
xmake b goldfish
bin/gf test tools/fmt/tests/liii/goldfmt-cache-test.scm
```

### 3.2 非确定性测试（性能验证）
```bash
# 清理缓存
rm -rf ~/.cache/goldfish/fmt

# 首次运行（无缓存）
cd ~/git/mogan
/home/da/git/goldfish/bin/gf fmt

# 再次运行（有缓存）
/home/da/git/goldfish/bin/gf fmt
```

## 2026-06-24 gf fmt 为 clang-format 添加文件缓存

### What
1. 把 `scheme-fmt.scm` 中的缓存函数抽取到新的共享模块 `goldfmt-cache.scm`，供 Scheme 与 C++ 格式化复用。
2. 在 `cpp-fmt.scm` 的 `format-cpp-file`、`format-one-cpp`、`check-cpp-file` 中增加缓存命中/写入逻辑：
   - 命中缓存时直接跳过 `clang-format` 调用；
   - 未命中时调用 `clang-format`，格式化结果与原内容一致后 touch 缓存；
   - `--check` 模式下同样使用缓存，检查通过后写入缓存。
3. `format-cpp-files` 返回 `(total updated cached)`，与 Scheme 格式化协议保持一致。
4. 新增 `tests/liii/goldfmt-cache-test.scm`，覆盖缓存路径计算、命中/写入逻辑。
5. 在 `(liii path)` 中新增 `path-contains?` 函数，用于跨平台判断路径是否为另一路径的子路径，并在 `goldfmt-cache-test.scm` 中用它替代字符串比较。

### Why
`gf fmt` 在格式化大量 C++ 文件时，每次都要调用 `clang-format` 进程，即使文件内容没有变化也无法跳过。`clang-format` 本身没有持久化缓存选项，因此在反复格式化同一个仓库时浪费大量时间。通过文件内容 SHA-256 缓存，可以把第二次格式化的耗时从数十秒降到亚秒级。

### How
缓存方案与 Scheme 格式化现有缓存一致：
- 缓存目录：`~/.cache/goldfish/fmt/<version>/`
- 缓存键：源文件内容的 SHA-256
- 缓存文件路径采用两级目录 `<前2位hash>/<剩余62位hash>`
- 缓存失效：Goldfish 版本变更会自动切换缓存目录；`.clang-format` 等配置变更需要手动清理缓存（与 Scheme 缓存语义一致）

在 `~/git/mogan` 上实测：
- 首次格式化：约 36 秒
- 再次格式化：约 0.3 秒
