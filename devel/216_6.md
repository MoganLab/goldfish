# [216_6] 将 (liii bag) 的测试用例迁移到 tests/liii/bag 目录

## 任务相关的代码文件
- tests/goldfish/liii/bag-test.scm
- tests/liii/bag-test.scm
- tests/liii/bag/*.scm

## 如何测试
```bash
# 1. 构建
xmake b goldfish

# 2. 运行测试
bin/gf tests/liii/bag-test.scm
bin/gf tests/liii/bag/*.scm
```

## 2026-03-30 拆分 bag-test.scm

### What
按照 216_2 的模式，将 bag-test.scm 拆分到 tests/liii/bag/ 目录：

1. 创建 tests/liii/bag-test.scm（入口文件，引用子目录测试）
2. 创建 tests/liii/bag/ 目录
3. 按函数拆分测试文件：
   - bag-test.scm
   - bag-adjoin-test.scm
   - bag-any-p-test.scm
   - bag-comparator-test.scm
   - bag-contains-p-test.scm
   - bag-copy-test.scm
   - bag-count-test.scm
   - bag-delete-test.scm
   - bag-delete-all-test.scm
   - bag-difference-test.scm
   - bag-disjoint-p-test.scm
   - bag-empty-p-test.scm
   - bag-every-p-test.scm
   - bag-find-test.scm
   - bag-intersection-test.scm
   - bag-member-test.scm
   - bag-p-test.scm
   - bag-replace-test.scm
   - bag-search-test.scm
   - bag-size-test.scm
   - bag-union-test.scm
   - bag-unfold-test.scm
   - bag-xor-test.scm
   - list-bang-test.scm
   - list-to-bang-test.scm
   - list-to-bag-test.scm
   - bag-comparison-test.scm（包含 bag=?, bag<?, bag>?, bag<=?, bag>=?）
4. 删除 tests/goldfish/liii/bag-test.scm

### Why
- 减少单个测试文件的大小
- 便于并行运行测试
- 符合项目新的测试组织规范

### How
参考 216_2 的实现方式：
- 每个测试文件从 `(import ...)` 开始，不包含协议头
- 使用 `(liii check)` 和 `(liii bag)`
- 保留原有的测试逻辑和断言
