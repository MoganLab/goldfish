# [0139] Linux 打包 CD 增加 pkg-config 依赖

## 任务相关的代码文件
- .github/workflows/package.yml

## 如何测试
```bash
xmake b goldfish
```
观察 CI package.yml 工作流是否正常执行。

## 2026-08-24 Linux 打包 CD 增加 pkg-config 依赖

### What
1. 在 `.github/workflows/package.yml` 的 apt 依赖安装列表中加入 `pkg-config`

### Why
cpr 配方（curl 8.21.0 源码，见 [200_82]）构建时依赖 pkg-config，Linux 打包环境缺少该工具会导致构建失败。
