# [0154] 修复 (liii vector) 相关内存越界与边界缺陷

## 任务相关的代码文件
- src/s7.c
- src/s7_liii_vector.c
- goldfish/scheme/base.scm
- goldfish/srfi/srfi-133.scm
- tests/liii/vector/float-vector-set-bang-test.scm
- tests/liii/vector/vector-filter-test.scm
- tests/liii/vector/vector-copy-bang-test.scm
- tests/liii/vector/reverse-vector-to-list-test.scm
- devel/0154.md

## 如何测试
```bash
xmake b goldfish
bin/gf fmt tests/liii/vector/float-vector-set-bang-test.scm
bin/gf tests/liii/vector/float-vector-set-bang-test.scm
bin/gf fmt tests/liii/vector/vector-filter-test.scm
bin/gf tests/liii/vector/vector-filter-test.scm
bin/gf fmt tests/liii/vector/vector-copy-bang-test.scm
bin/gf tests/liii/vector/vector-copy-bang-test.scm
bin/gf fmt tests/liii/vector/reverse-vector-to-list-test.scm
bin/gf tests/liii/vector/reverse-vector-to-list-test.scm
```

预期：全部测试用例通过，0 failed。

## 2026-09-08 修复 reverse-vector->list 参数边界校验缺失问题

### What
1. 在 `goldfish/srfi/srfi-133.scm` 的 `reverse-vector->list` 中增加参数类型与区间范围检查：
   - 检查 `vec` 是否为 `vector?`，`start` 与 `end` 是否为 `integer?`；
   - 检查 `0 <= start <= len` 与 `0 <= end <= len`；
   - 检查 `start <= end`，非法时统一抛出 `out-of-range`。

### Why
此前 `reverse-vector->list` 缺乏显式边界防线。当传入非法参数时，依靠内部循环递增 `i` 直到下标越界抛错，不仅在逻辑上不严谨，且无法在函数入口拦截非法输入。

### How
在进入转换循环前增加 `unless`/`when` 防御分支，非法类型抛出 `wrong-type-arg`，越界或倒挂区间抛出 `out-of-range`。

## 2026-09-08 为 reverse-vector->list 补充越界与非法区间边界测试用例

### What
1. 在 `tests/liii/vector/reverse-vector-to-list-test.scm` 中增加 `(reverse-vector->list v -1 0)` 和空向量非法区间 `(reverse-vector->list #() 1 0)` 测试用例。

### Why
`goldfish/srfi/srfi-133.scm` 的 `reverse-vector->list` 未在入口校验参数类型与 `start`/`end` 的范围，当 `start > end` 或 `start < 0` 时，依靠递增 `i` 穿透至循环体内触发内部越界，行为不够严谨规范。

## 2026-09-08 修复 vector-copy! 遗漏 (< start 0) 边界校验问题

### What
1. 在 `goldfish/scheme/base.scm` 的 `vector-copy!` 入口边界校验中增加 `(< start 0)` 分支。

### Why
此前仅校验了 `(< at 0)` 与 `(< end 0)`，遗漏了 `(< start 0)`。当传入负数 `start` 且 `end = 0` 时，入口检查无法将其拦截为 `out-of-range`，而是直接放行至循环体内部。

### How
在入口 `or` 判断中补齐 `(< start 0)`，确保非法负数索引在函数入口处直接抛出 `out-of-range`。

## 2026-09-08 为 vector-copy! 补充负数 start 边界测试用例

### What
1. 在 `tests/liii/vector/vector-copy-bang-test.scm` 中补充 `(vector-copy! b 0 a -1 0)` 边界用例。

### Why
`goldfish/scheme/base.scm` 中的 `vector-copy!` 入口边界判断包含了 `(< at 0)`、`(< end 0)`、`(> start end)` 等，但遗漏了 `(< start 0)`。当传入负数 `start` 且 `end = 0` 时，入口校验直接放行，错误地穿透至内部拷贝循环。

## 2026-09-08 修复 vector-filter 提前解除 GC 栈保护导致的悬空指针隐患

### What
1. 在 `src/s7_liii_vector.c` 的 `g_vector_filter` 中，将 `s7_gc_unprotect_via_stack(sc, anchor)` 移动到分配最终收缩向量 `exact` 并完成元素拷贝之后。

### Why
此前 `g_vector_filter` 在完成遍历后立即解除了对 `anchor` 的栈保护。当筛选出的元素数量小于原向量长度时，需要调用 `s7_make_vector(sc, count)` 分配新向量。该内存分配极易触发 GC，而此时存放中间结果的向量 `result` 处于无保护状态，可能被 GC 回收或破坏，后续从 `result` 拷贝数据将产生悬空指针访问甚至段错误。

### How
将取消保护的时机延迟到函数返回前（包括全量保留的直接返回分支和分配收缩向量的返回分支），确保中间结果向量在整个执行周期内始终被 GC Root 追踪。

## 2026-09-08 增加 vector-filter 大向量 GC 场景测试用例

### What
1. 在 `tests/liii/vector/vector-filter-test.scm` 中增加 10000 元素大向量的过滤测试用例，覆盖分配收缩结果向量时的 GC 场景。

### Why
`src/s7_liii_vector.c` 的 `g_vector_filter` 在调用 `s7_make_vector(sc, count)` 分配最终精确尺寸的向量之前，过早执行了 `s7_gc_unprotect_via_stack(sc, anchor)`。若分配触发 GC，中间向量 `result` 缺乏栈保护，存在被提前回收/内存破坏的隐患。

## 2026-09-08 修复 float-vector-set! 条件分支触发未校验越界写导致的段错误

### What
1. 在 `src/s7.c` 中修改 `float_vector_set_chooser`，移除不安全的 `find_matching_ref` 投机优化，统一返回带安全边界检查的 `sc->fv_set_3`。
2. 在 `tests/liii/vector/float-vector-set-bang-test.scm` 中增加测试用例，覆盖条件分支内包含 `float-vector-ref` 时索引越界的场景，验证抛出 `out-of-range` 错误而非段错误崩溃。
3. 移除复现文件 `demo/crash/c46-float-vector-set-branch-oob.scm`。

### Why
`float_vector_set_chooser` 此前通过 `find_matching_ref` 静态扫描子表达式，当发现存在 `float-vector-ref` 时便误以为索引已被检查，从而选用 `g_fv_set_unchecked`。若该 getter 处于未执行的条件分支中，越界检查会被完全跳过，向非法地址写入内存导致 SIGSEGV 段错误。

### How
对齐 `int_vector_set_chooser` 和 `complex_vector_set_chooser` 的实现策略，三参数调用时直接返回 `sc->fv_set_3`，从根源上杜绝未检查的越界写。

## 2026-09-08 复现 float-vector-set! 条件分支触发优化器未校验越界写导致的崩溃

### What
1. 新增 `demo/crash/c46-float-vector-set-branch-oob.scm` 复现脚本，验证 `float-vector-set!` 在值表达式包含条件分支中的 `float-vector-ref` 时，误触发底层 `g_fv_set_unchecked` 导致 SIGSEGV 段错误（退出码 139）。

### Why
优化器 `float_vector_set_chooser` 依赖 `find_matching_ref` 静态扫描子表达式。当子表达式中包含 `(float-vector-ref v i)` 但处于未执行的分支（如 `if` 假分支）时，优化器误判 getter 已执行过边界检查，从而错误地选择无边界保护的 `g_fv_set_unchecked`，在索引越界时直接非法写入内存导致崩溃。

### How
定义函数 `(float-vector-set! v i (if #t 1.0 (float-vector-ref v i)))`，传入超大索引 `100000000` 调用触发崩溃。
