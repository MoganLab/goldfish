# [216_39] 拆分 packrat-test.scm 到 tests/liii/packrat/ 目录

## 任务相关的代码文件
- tests/goldfish/liii/packrat-test.scm (删除)
- tests/liii/packrat-test.scm (新建，顶层入口)
- tests/liii/packrat/ 目录下 25 个测试文件

## 如何测试
```bash
xmake b goldfish
bin/gf fix tests/liii/packrat/
bin/gf fix tests/liii/packrat-test.scm

# 运行所有测试
for f in $(find tests/liii/packrat -maxdepth 1 -type f -name "*-test.scm" | sort); do
  echo "Testing: $f"
  bin/gf "$f" || exit 1
done
```

## 2026-04-01 拆分 packrat-test.scm

### What
将原有的 `tests/goldfish/liii/packrat-test.scm` 文件按照 Unit_Test_ZH.md 规范拆分为两层结构：

1. **创建顶层入口文件** `tests/liii/packrat-test.scm`
   - 模块导览说明
   - 常见用法示例
   - 函数分类索引

2. **创建 25 个单独函数测试文件** 在 `tests/liii/packrat/` 目录：

   parse-result 相关：
   - `make-result-test.scm`
   - `parse-result-p-test.scm`
   - `parse-result-successful-p-test.scm`
   - `parse-result-semantic-value-test.scm`
   - `parse-result-next-test.scm`
   - `make-expected-result-test.scm`
   - `make-message-result-test.scm`

   parse-position 相关：
   - `make-parse-position-test.scm`
   - `parse-position-p-test.scm`
   - `parse-position-file-test.scm`
   - `parse-position-line-test.scm`
   - `parse-position-column-test.scm`

   parse-results 相关：
   - `base-generator-to-results-test.scm`
   - `parse-results-p-test.scm`
   - `parse-results-token-kind-test.scm`
   - `parse-results-token-value-test.scm`

   parse-error 相关：
   - `make-error-expected-test.scm`
   - `make-error-message-test.scm`
   - `parse-error-p-test.scm`
   - `parse-error-position-test.scm`

   combinator 相关：
   - `packrat-check-base-test.scm`
   - `packrat-or-test.scm`
   - `packrat-check-test.scm`
   - `packrat-unless-test.scm`

   解析器构造：
   - `packrat-parser-test.scm`

3. **删除旧测试文件** `tests/goldfish/liii/packrat-test.scm`

### Why
- 遵循项目新的测试组织规范
- 每个函数独立测试文件，便于维护和定位问题
- 顶层入口文件提供模块整体说明和快速导航

### How
- 严格按照 Unit_Test_ZH.md 的两层结构规范
- 文件名按照符号转义规则（如 `?` -> `-p`）
- 保留原有测试逻辑和用例
- 为每个测试文件添加完整的文档字符串（语法、参数、返回值、描述）
