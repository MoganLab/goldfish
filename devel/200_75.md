# char, string 支持 Unicode

> 本文档遵循 [x_y.md](x_y.md) 中定义的格式规范。

## 完善char的unicode支持（`913e9889`）
## char?支持中文字符（`a34f16f8`）

## 实现 (liii string-cursor)

### What
新增 `(liii string-cursor)` 库，提供基于 cursor（游标）的字符串操作，使字符串操作支持 Unicode 字符级别语义，同时**不修改 `(liii string)` 的现有语义**。

`(liii string)` 保持字节级语义不变（`string-length` 返回字节数，`string-ref` 按字节索引），新增的 `(liii string-cursor)` 提供字符级语义：

- `string-cursor-start/end/next/prev` — cursor 导航
- `string-ref/cursor` — 按 cursor 取完整 Unicode 字符
- `substring/cursors` — 按 cursor 截取子串
- `string-index/string-skip` — 搜索返回 cursor
- `string-take/string-drop` — 按字符数截取
- `string-fold/string-fold-right` — 字符级 fold

### Why
当前 `(liii string)` 对 UTF-8 字符串的操作是按字节而非按字符：

```scheme
(string-length "中文")    ; => 6  （字节长度）
(string-ref "中文" 0)     ; => #\xE4 （UTF-8 首字节，不是 #\中）
```

这导致：
1. 用户无法直观地对 Unicode 字符串做"取第 N 个字符"的操作
2. `string-split`、`string-take` 等函数在中文内容上会按字节切割，破坏字符编码
3. 与 `(liii unicode)` 提供的 `utf8-string-length`、`utf8-substring` 形成割裂

新增 `(liii string-cursor)` 是为了：
- 保持 `(liii string)` 向后兼容
- 提供一套完整的、字符级语义的字符串操作 API
- 遵循 SRFI-130（Cursor-based string library）规范

### How

#### cursor 的表示
cursor = **字节索引**（整数），和底层 UTF-8 存储一致。

原因：
- 和 `(liii unicode)` 的 `bytevector-advance-utf8` 完全兼容
- `string-cursor-next` 是 O(1)（直接跳过一个 UTF-8 字符的字节数）
- 如果 cursor = 字符索引，每次都要 O(n) 遍历转换，性能差

用户看到的 cursor 是透明对象，不暴露是字节还是字符索引。

#### 核心实现思路

1. **内部转换**：字符串操作先将 `string->utf8` 转成 bytevector，用 `bytevector-advance-utf8` 做字符级导航，最后用 `utf8->string` 转回。

2. **导航函数**：
   ```scheme
   (define (string-cursor-next str cursor)
     (let ((bv (string->utf8 str)))
       (bytevector-advance-utf8 bv cursor (bytevector-length bv))))
   ```

3. **取值函数**：
   ```scheme
   (define (string-ref/cursor str cursor)
     (let* ((bv (string->utf8 str))
            (next (bytevector-advance-utf8 bv cursor (bytevector-length bv)))
            (char-bv (bytevector-copy bv cursor next)))
       (string-ref (utf8->string char-bv) 0)))
   ```

4. **搜索函数**（如 `string-index`）：在 bytevector 上逐字符（用 `bytevector-advance-utf8`）遍历，匹配成功后返回 cursor（字节位置）。

#### 与 (liii string) 的关系

| 库 | 语义 | 示例 `"中文"` |
|---|---|---|
| `(liii string)` | 字节级 | `string-length` => 6, `string-ref` 取字节 |
| `(liii string-cursor)` | 字符级 | `string-cursor-diff` => 2, `string-ref/cursor` 取字符 |

两者独立，用户按需选用。

#### 参考规范
- SRFI-130: Cursor-based string library
- `(liii unicode)` 已提供的工具：`bytevector-advance-utf8`, `utf8-string-length`, `utf8-substring`

#### 测试策略
每个 cursor 函数对应独立测试文件：
- `tests/liii/string-cursor/string-cursor-start-end-test.scm`
- `tests/liii/string-cursor/string-cursor-next-prev-test.scm`
- `tests/liii/string-cursor/string-ref-cursor-test.scm`
- `tests/liii/string-cursor/string-index-cursor-test.scm`
- ...

测试中必测中文、emoji（多字节字符）、ASCII 混合场景。