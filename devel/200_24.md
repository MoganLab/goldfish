# [200_24] 迁移 (scheme char) 相关底层实现，从 s7.c 到 s7_scheme_char.c

## 任务相关的代码文件
- src/s7.c
- src/s7_scheme_char.c
- src/s7_scheme_char.h
- xmake.lua
- devel/200_24.md

## 如何测试
```
xmake f -vyD
xmake b goldfish
bin/goldfish tests/goldfish/scheme/char-test.scm
```

## 2026/02/26 迁移 (scheme char) 相关底层实现，从 s7.c 到 s7_scheme_char.c
### What
将 `(scheme char)` 相关函数从 `s7.c` 迁移到 `s7_scheme_char.c`，包括：
1. 迁移 `char->integer`/`integer->char`、`char-upcase`/`char-downcase`、`char-alphabetic?`/`char-numeric?`/`char-whitespace?`、
   `char-upper-case?`/`char-lower-case?`、`char=?`/`char<?`/`char>?`/`char<=?`/`char>=?`、
   `char-ci*` 系列、`char-position` 及其直接依赖实现
2. 新增 `s7_scheme_char.h`，补充迁移函数声明并与 `s7.c` 对接
3. 在 `s7.c` 中移除对应实现，改为包含 `s7_scheme_char.h`
4. 在 `xmake.lua` 中新增 `src/s7_scheme_char.c` 编译入口
5. 保持函数注册、优化路径与现有行为不变
